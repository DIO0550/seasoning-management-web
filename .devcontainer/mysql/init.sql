-- Initialization script for the database
-- This script will be executed when the MySQL container starts for the first time.

CREATE DATABASE IF NOT EXISTS seasoning_management_db;

-- You can add more initialization commands here, for example:
-- CREATE TABLE IF NOT EXISTS your_table_name (
--   id INT AUTO_INCREMENT PRIMARY KEY,
--   name VARCHAR(255) NOT NULL
-- );

-- GRANT ALL PRIVILEGES ON seasoning_management_db.* TO 'user'@'%';
-- FLUSH PRIVILEGES;
